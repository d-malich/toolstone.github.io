let filter_content = document.querySelectorAll('.product_filter .filter_content')

let visible_btn = document.querySelectorAll('.product_filter .filter_header .icon-open')

let filter_content_height

// visible_btn.forEach((b, i) => b.addEventListener('click', () => {
//   console.log(filter_content[i].offsetHeight);
//   filter_content_height = filter_content[i].offsetHeight
// }))